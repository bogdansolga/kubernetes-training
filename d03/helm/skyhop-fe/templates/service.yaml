apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: skyhop-fe
spec:
  type: ClusterIP
  selector:
    app: skyhop-fe
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
