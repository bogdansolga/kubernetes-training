# Day 3 · monitoring — exposes Grafana on :3000 (in-cluster; port-forward to reach it).
apiVersion: v1
kind: Service
metadata:
  name: grafana
  labels:
    app: grafana
spec:
  type: ClusterIP
  selector:
    app: grafana
  ports:
    - name: http
      port: 3000
      targetPort: 3000
