# Day 3 · monitoring — kube-state-metrics (KSM) reads Kubernetes objects via the API, so it needs
# a ServiceAccount bound to a read-only ClusterRole (ksm-clusterrole.yml).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kube-state-metrics
